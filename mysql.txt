// 查看版本号
    select version();
// 显示使用者和日期
    select user(),select now();
// as 关键字设置字段名
    select version() as '版本号';
// 执行返回计算结果
    select (20*5)+100;
// 多行操作
    select
    user()
    ,
    now()
    ;
// 窗口退出
    exit;
    quit;
// 命令取消
    see
    \c
// 显示当前存在的数据库
   选择你需要的数据库
   查看当前选择的数据库
   查看一张表的所有内容
   根据数据库设置中文编码
   创建一个数据库